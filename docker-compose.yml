version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: rec_sakh
      POSTGRES_PASSWORD: rec_sakh_dev
      POSTGRES_DB: rec_sakh
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rec_sakh"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  typesense:
    image: typesense/typesense:26.0
    ports:
      - "8108:8108"
    environment:
      TYPESENSE_API_KEY: rec_sakh_typesense_dev_key
      TYPESENSE_DATA_DIR: /data
    volumes:
      - typesensedata:/data
    command: >
      --data-dir /data
      --api-key rec_sakh_typesense_dev_key
      --listen-port 8108
      --enable-cors

volumes:
  pgdata:
  redisdata:
  typesensedata:
